<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Amazoom | De A à M{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    {% block stylesheets %}
        {#{{ encore_entry_link_tags('app') }}#}
    {% endblock %}

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.6.0/dist/umd/popper.min.js" integrity="sha384-KsvD1yqQ1/1+IA7gi3P0tyJcT3vR+NdBTt13hSJ2lnve8agRGXTTyNaBYmCR/Nwi" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.min.js" integrity="sha384-nsg8ua9HAw1y0W1btsyWgBklPnCUAFLuTMS2G72MMONqmOymq585AcH49TLBQObG" crossorigin="anonymous"></script>
    {% endblock %}
</head>
<body class="lightmode_body" id="body">
<style>
    .darkmode_nav{
        background-color:#272a2c
    }

    .darkmode_container{
        background-color:#222527
    }
    .darkmode_body{
        background-color:#292d2e;
        color:#d1cdc7

    }

    .lightmode_nav{
        background-color:#E5E4E4
    }

    .lightmode_container{
        background-color:#EEEBEB
    }
    .lightmode_body{
        background-color:#E1E0E0;
        color:black

    }
</style>
<script>
    let dark = false;
    function switch_color()
    {
        if (dark)
        {
            dark = !dark;
            document.getElementById('body').classList.remove("darkmode_body");
            document.getElementById('body').classList.add("lightmode_body");
            document.querySelectorAll('#container').forEach(e => e.classList.remove("darkmode_container"));
            document.querySelectorAll('#container').forEach(e => e.classList.add("lightmode_container"));
            document.querySelectorAll('#but').forEach(e => e.classList.remove("darkmode_body"));
            document.querySelectorAll('#but').forEach(e => e.classList.add("lightmode_body"));
            document.querySelectorAll('#nav').forEach(e => e.classList.remove("darktmode_nav"));
            document.querySelectorAll('#nav').forEach(e => e.classList.add("lightmode_nav"));
            document.querySelectorAll('#table').forEach(e => e.classList.remove("table-dark"));
            document.querySelectorAll('#table').forEach(e => e.classList.add("table-light"));            
        }
        else{
            dark = !dark;
            document.getElementById('body').classList.remove("lightmode_body");
            document.getElementById('body').classList.add("darkmode_body");
            document.querySelectorAll('#container').forEach(e => e.classList.remove("lightmode_container"));
            document.querySelectorAll('#container').forEach(e => e.classList.add("darkmode_container"));
            document.querySelectorAll('#but').forEach(e => e.classList.add("darkmode_body"));
            document.querySelectorAll('#but').forEach(e => e.classList.remove("lightmode_body"));
            document.querySelectorAll('#nav').forEach(e => e.classList.remove("lightmode_nav"));
            document.querySelectorAll('#nav').forEach(e => e.classList.add("darkmode_nav"));
            document.querySelectorAll('#table').forEach(e => e.classList.add("table-dark"));
            document.querySelectorAll('#table').forEach(e => e.classList.remove("table-light")); 
        }
    }
</script>
{{ knp_menu_render('main', {'template': 'menu/_menu.html.twig', style: 'horizontal'}) }}


{% block content %}{% endblock %}

</body>

</html>